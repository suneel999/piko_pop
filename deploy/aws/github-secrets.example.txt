# Add these in GitHub: Settings -> Secrets and variables -> Actions

# Shared (same key can deploy both servers if you use one instance first)
AWS_SSH_PRIVATE_KEY = contents of deploy_key.pem (private key)

# Staging server (Lightsail/EC2 public IP or domain)
AWS_STAGING_HOST = 3.110.xxx.xxx
AWS_STAGING_USER = ubuntu
AWS_STAGING_PATH = /var/www/piko_pop

# Production server (later, separate instance recommended)
AWS_PRODUCTION_HOST = 3.110.yyy.yyy
AWS_PRODUCTION_USER = ubuntu
AWS_PRODUCTION_PATH = /var/www/piko_pop
